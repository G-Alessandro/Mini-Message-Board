extends layout

block content
  form(action="/new", method="post")
    label(for="user") Author Name:
    input(type="text", id="user", name="user", required)
    br
    label(for="text") Message Text:
    input(type="text", id="text", name="text", required)
    br
    button(type="submit") Submit